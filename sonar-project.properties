# The example RP and Playwright relying-party fixture are development tools.
# They intentionally support loopback HTTP so flows can be exercised locally;
# production server code remains covered by SonarCloud.
sonar.exclusions=example-rp/**,tests/**
